@inherits DotNetNuke.Web.Mvc.Framework.DnnWebViewPage
@using DotNetNuke.Web.Mvc.Helpers
@if (Dnn.ModuleContext.IsEditable) {
	var lastItem = (string) Session["r7_University_LastItem"];
	if (!string.IsNullOrEmpty (lastItem)) {
		<script type="text/javascript">
		(function ($, window, document) {
			$(document).ready(function() {
				var elem = $(".DnnModule-@Dnn.ModuleContext.ModuleId #@lastItem");
				if (elem.length > 0) {
					var offset = elem.offset ();
					window.scrollTo (offset.left, offset.top - window.innerHeight/4);
					$(elem).addClass ("u8y-last-item");
				}
			});
		})(jQuery, window, document);
		</script>
	}
}
else {
	Session["r7_University_LastItem"] = null;
}